/* tslint:disable:no-unused-variable */

import { UserFindPipe } from './user-filter.pipe';

describe('Pipe: UserFiltere', () => {
  it('create an instance', () => {
    let pipe = new UserFindPipe();
    expect(pipe).toBeTruthy();
  });
});
