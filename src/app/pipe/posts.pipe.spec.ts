/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PostsPipe } from './posts.pipe';

describe('Pipe: Postse', () => {
  it('create an instance', () => {
    let pipe = new PostsPipe();
    expect(pipe).toBeTruthy();
  });
});
